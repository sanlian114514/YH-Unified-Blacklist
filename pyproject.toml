[project]
name = "yh-unified-blacklist"
version = "0.1.0"
description = "云湖反非联统一黑名单API(机器人)"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "protobuf-py>=0.4.0",
    "uvicorn>=0.52.4",
    "websockets>=17.1",
]
